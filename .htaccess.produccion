# OBSOLETO — no usar.
#
# Este archivo era una variante del .htaccess para producción, pero quedó
# desactualizado (apuntaba a RewriteBase /petersen2025/) y deploy.sh nunca lo
# copió al servidor: producción siempre recibió el .htaccess principal.
#
# El .htaccess principal ya no declara RewriteBase, por lo que funciona tanto en
# la raíz del dominio (producción) como en la subcarpeta /petersen (local).
# No hace falta mantener dos versiones: editá únicamente .htaccess.
